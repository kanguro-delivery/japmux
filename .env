# .env.sample - Example file for environment variables
# Copy this file to .env.local (or .env.<environment>) and fill in the necessary values.
# DO NOT commit .env files with actual values to Git.

# Base URL for the API
# Define the URL where the backend API is listening.
# Example for local development: http://localhost:8000/api
# Example for production: https://your-api.com/api
NEXT_PUBLIC_API_URL=http://localhost:3000
# NEXT_PUBLIC_API_URL=https://api.japm.test.kanguro.com
NEXT_PUBLIC_DEFAULT_LANGUAGE_CODE=es-ES
